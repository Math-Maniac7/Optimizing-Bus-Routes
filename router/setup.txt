We're going to develop on linux, then cross compile to WASM, which should be able to run on whatever OS you like that supports WASM. 

Dependencies:
 - libcurl : C++ CURL library
    sudo apt-get install libcurl4-openssl-dev




